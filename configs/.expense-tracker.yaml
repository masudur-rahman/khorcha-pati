telegram:
    user: masudur_rahman
database:
    type: postgres
#    sqlite:
#        syncToDrive: true
#        disableSyncFromDrive: true
    postgres:
        name: expense
        host: localhost
        port: 5432
        user: postgres
        password: postgres
        sslmode: disable
cache:
    type: redis #map
    redis:
        host: localhost
        port: "6379"
        password: redis
