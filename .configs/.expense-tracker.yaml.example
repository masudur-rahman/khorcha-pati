telegram:
    user: masudur_rahman
database:
    type: postgres # options: (sqlite, postgres, arangodb, supabase)
#    sqlite:
#        syncToDrive: true
#        disableSyncFromDrive: true
    postgres:
        name: expense
        host: localhost
        port: 5432
        user: postgres
        password: postgres
        sslmode: disable
cache:
    type: map #redis
    redis:
        host: localhost
        port: "6379"
        password: redis
